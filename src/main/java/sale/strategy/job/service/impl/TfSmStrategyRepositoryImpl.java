package sale.strategy.job.service.impl;

import sale.strategy.job.service.TfSmStrategyRepository;
import sale.strategy.job.bean.TfSmStrategy;
import org.springframework.stereotype.Service;

@Service("tfSmStrategyRepository")
public class TfSmStrategyRepositoryImpl  implements TfSmStrategyRepository<TfSmStrategy>
{
    // TODO: 18/6/1  
}